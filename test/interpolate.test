// embedded source
Config
    .fetch({
        config: {
            foo: 'fooVal',
            bar: '#[foo]',
            sub: {
                val: '#[bar]'
            }
        }
    })
    .done(config => deepEq_(config.toJSON(), {
        foo: 'fooVal',
        bar: 'fooVal',
        sub: {
            val: 'fooVal'
        }
    }));
    
    